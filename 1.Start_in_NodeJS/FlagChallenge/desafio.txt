
    Crie 2 arquivos JavaScript

    * O primeiro arquivo, irá exportar uma função chamada getFlagValue()
    * Essa função recebera um argumento do tipo String
    * Essa função tambem devera buscar dentro do array flag.argv a flag deseja, que é a String e retornar o valor da Flag
    
    * O segundo arquivo, irá importar a função e passar a flag desejada
    * Iremos rodar no terminal o segundo arquivo passando as flags --name e --greeting
    * Deve ser impresso no terminal a saudação e o nome da pessoa
